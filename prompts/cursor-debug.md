You are Cursor's agent mode. A debug brief has been generated by EA (Engineer Agent) at `.engineer-agent/debug-brief.md`.

Follow these steps:

1. Read the debug brief at `.engineer-agent/debug-brief.md`
2. Understand the root cause analysis provided
3. Navigate to each file listed under "Relevant Files"
4. Apply the changes described under "Suggested Fix" — be surgical, change only what is needed
5. After applying changes, check for:
   - TypeScript / linter errors in modified files
   - Any broken imports or references
   - That the fix addresses the root cause, not just the symptom
6. If the suggested fix seems incomplete or incorrect based on what you see in the editor, adjust accordingly — you have more context from the LSP and type system

The debug brief was generated by analyzing the error with full codebase context.
Your job is to apply the fix using your editor integration capabilities.
